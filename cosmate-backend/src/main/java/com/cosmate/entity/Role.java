package com.cosmate.entity;

public enum Role {
    SUPERADMIN,
    ADMIN,
    COSPLAYER,
    PROVIDER, // legacy alias kept for backward compatibility
    PROVIDER_RENTAL,
    PROVIDER_PHOTOGRAPH,
    PROVIDER_EVENT_STAFF,
    STAFF
}
